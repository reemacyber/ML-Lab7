# ML-Lab7
The dataset includes advertising spending (TV, Radio, Newspaper) and sales results. The data was cleaned and used to train a logistic regression model, which was then evaluated on test data and achieved high accuracy.
